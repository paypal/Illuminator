language: objective-c
install: bundle install --jobs=3 --retry=3 --gemfile SampleApp/Gemfile
script:
   - rubocop -D --only Style/MethodName,Style/VariableName,Style/RedundantSelf .
   - cd SampleApp
   - pod install
   - ruby smokeTests.rb